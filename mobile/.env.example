# API 서버 주소 (필수). 백엔드가 3001에서 떠 있으면 :3001 로 설정
# Windows: ipconfig → IPv4 주소 확인 후 아래를 실제 PC IP로 수정
# 복사: copy .env.example .env  후  npx expo start -c  로 재시작
EXPO_PUBLIC_API_BASE_URL=http://192.168.0.2:3000
