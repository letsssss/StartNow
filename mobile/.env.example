# API 서버 주소 (필수). 앱이 이 URL로 /api/structure 등을 호출합니다.
# 로컬: Windows에서 ipconfig → IPv4 주소 확인 후 아래를 실제 PC IP로 수정
# EAS Build: eas.json 해당 프로필의 env에 넣거나 EAS Secret으로 EXPO_PUBLIC_API_BASE_URL 설정
# 복사: copy .env.example .env  후  npx expo start -c  로 재시작
EXPO_PUBLIC_API_BASE_URL=http://192.168.0.2:3000
